<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('includes/head', { title: 'Update post' }) %>
    <link rel="stylesheet" href="/styles/forms.css" />
  </head>

  <body>
    <%- include('includes/header') %>
    <main>
      <h1>Update post</h1>
      <form action="/posts/<%= post._id %> " method="POST">
        <!-- The above action is the post route we created on the blog.js file. -->
        <div class="form-control">
          <label for="title">Title</label>
          <input
            type="text"
            id="title"
            name="title"
            value="<%= post.title %> "
            required
          />
          <!-- The value attribute will pre populate the form with the already entered data. -->
        </div>
        <div class="form-control">
          <label for="summary">Summary</label>
          <input
            type="text"
            id="summary"
            name="summary"
            value="<%= post.summary %> "
            required
            maxlength="255"
          />
          <!-- The value attribute will pre populate the form with the already entered data. -->
        </div>
        <div class="form-control">
          <label for="content">Post Content</label>
          <textarea id="content" name="content" required rows="5"><%= post.body %> </textarea>
          <!-- We shouldn't add a white space in here as it would be a part of the output. -->
        </div>
        <button class="btn">Update Post</button>
      </form>
    </main>
  </body>
</html>
